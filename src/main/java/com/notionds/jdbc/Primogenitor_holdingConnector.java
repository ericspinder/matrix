package com.notionds.jdbc;

import dev.inward.matrix.datum.Identity;
import dev.inward.matrix.datum.fact.notion.*;
import dev.inward.matrix.datum.fact.notion.concept.Context;

import java.lang.ref.ReferenceQueue;

public class Primogenitor_holdingConnector extends Primogenitor<Industry_forConnector,Connector,Aspect_ofConnector, Identity.Ego<Context.JVM>,Context.JVM,Omnibus_forConnector, Primogenitor_holdingConnector> {
    public Primogenitor_holdingConnector(Connector connector, ReferenceQueue<Connector> referenceQueue, Omnibus_forConnector omnibus, Aspect_ofConnector aspect) {
        super(connector, referenceQueue, omnibus, aspect);
    }
}
