package dev.inward.matrix.http;

import dev.inward.matrix.Domain;
import dev.inward.matrix.Library;

public class Library_ofHttp extends Library<Scheme_ofHttp,Library_ofHttp,String> {
    public Library_ofHttp(Scheme_ofHttp scheme, Domain domain) {
        super(scheme, domain);
    }
}
