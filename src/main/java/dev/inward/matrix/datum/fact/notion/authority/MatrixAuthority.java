package dev.inward.matrix.datum.fact.notion.authority;

import dev.inward.matrix.datum.Identity;

public class MatrixAuthority extends Authority {

    public MatrixAuthority(Identity.Ghost id) {
        super(id);
    }
}
