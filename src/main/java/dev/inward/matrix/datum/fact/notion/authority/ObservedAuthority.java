package dev.inward.matrix.datum.fact.notion.authority;

import dev.inward.matrix.datum.Identity;

public class ObservedAuthority extends Authority {

    public ObservedAuthority(Identity.Ghost ghost) {
        super(ghost);
    }

}
