package dev.inward.matrix.realm;

import dev.inward.matrix.Domain;
import dev.inward.matrix.Library;

public class Library_ofRealm extends Library<Scheme_ofRealm,Library_ofRealm,String> {

    public Library_ofRealm(Scheme_ofRealm scheme, Domain domain) {
        super(scheme, domain);
    }

}
